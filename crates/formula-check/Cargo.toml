[package]
name = "formula-check"
version = "0.0.1"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
formula-core = { path = "../formula-core" }
num-bigint.workspace = true
