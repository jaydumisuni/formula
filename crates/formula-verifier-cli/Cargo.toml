[package]
name = "formula-verifier-cli"
version = "0.0.1"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
formula-check = { path = "../formula-check" }
num-bigint.workspace = true
