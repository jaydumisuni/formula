# Design authority

The repository name `formula` is temporary and is not the final product identity.

## Frozen milestones

- [D1 — Mathematical Constitution](2026-08-28-d1-mathematical-constitution.md) — frozen design milestone defining the mathematical authority model, six durable artifact classes, Problem/Campaign Compiler, Discovery Fabric, Certification + Promotion boundary, Execution/Realization boundary, and constitutional laws.
- [D2 — Operational Mathematical Machine](2026-08-28-d2-operational-mathematical-machine.md) — frozen operational milestone defining package/capability contracts, Theory Profiles, generation-scoped capability closure, CandidateSpaces, Campaign/Work Cell IR, shared mathematical facts, search economy, certificate routing, realization planning, and the canonical First-Light proof campaign.
- [D3 — First-Light Build Architecture](2026-08-28-d3-first-light-build-architecture.md) — frozen build-architecture milestone selecting the smallest concrete local CPU implementation stack, exact blind target suite, CandidateSpace/checker boundaries, persistence/content identity layout, promotion transaction, realization validation route, negative controls, proof manifest, and build order required to prove D1 + D2.

## Current design rule

Research under `../research/` is the evidence authority behind the design. Broad research is no longer the lead activity. New research should be opened only when a design or implementation decision is unsupported, contradicted, or materially under-specified by the preserved research.

## Current implementation boundary

D1 + D2 + D3 are now the frozen authority for First Light.

The canonical First-Light implementation must remain local, CPU-only, model-free, network-free during execution, independently checked, and bound to exact Universe/package/grammar/source digests. Advanced infrastructure is downstream unless an explicit D1/D2 obligation cannot be proven without it.

## Next milestone

F0 — First-Light implementation campaign.

F0 should implement D3 build stages B01–B13 and produce the first independently replayable First-Light proof manifest with every constitutional PASS marker. A broader long-term roadmap should remain downstream of that executable evidence.
